#   _ __   ___  ___  __ _ _ __   __ _
#  | '_ \ / _ \/ __|/ _` | '_ \ / _` |
#  | | | | (_) \__ \ (_| | | | | (_| |
#  |_| |_|\___/|___/\__,_|_| |_|\__,_|

image: node:22.22.0

stages:
  - test
  - trigger
  - deploy

variables:
  GIT_STRATEGY: fetch
  GIT_DEPTH: "0"
  KIT_CHANGED: "0"
  DOCS_CHANGED: "0"

.pnpm_setup: &pnpm_setup
  - corepack enable
  - npm install -g corepack@latest
  - corepack prepare pnpm@10.28.0 --activate

before_script: *pnpm_setup

.pipeline_rules: &pipeline_rules
  - if: $CI_MERGE_REQUEST_ID
  - if: $CI_COMMIT_BRANCH == $CI_DEFAULT_BRANCH
  - if: $CI_COMMIT_TAG && $CI_COMMIT_TAG != "latest"

include:
  - local: .gitlab/ci/release.yml
  - local: .gitlab/ci/generated-clients.yml

detect-changes:
  stage: trigger
  needs:
    - job: check-generated-clients
  script:
    - bash .gitlab/ci/detect-changes.sh
  artifacts:
    paths:
      - orchestrator.yml
    expire_in: 1 hour
    reports:
      # Injects KIT_CHANGED, DOCS_CHANGED, etc. into jobs that need this job
      dotenv: changes.env
  rules: *pipeline_rules

# Single trigger: orchestrator child uses local: path to each package's .gitlab-ci.yml.
# Forward dotenv variables (KIT_CHANGED, DOCS_CHANGED, etc.) so orchestrator rules see them.
run-child-pipelines:
  stage: trigger
  needs:
    - job: detect-changes
      artifacts: true
  trigger:
    include:
      - artifact: orchestrator.yml
        job: detect-changes
    strategy: depend
    forward:
      pipeline_variables: true
  rules: *pipeline_rules
